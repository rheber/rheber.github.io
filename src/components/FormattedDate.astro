---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-au', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
<style>
	time {
		font-size: 16px;
		line-height: 20px;
	}
</style>